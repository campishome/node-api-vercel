{
    "version":2,
    "builds":[
        {
            "src":"try.ts",
            "use":"@now/node"
        }
    ],
    "routes":[
        {
            "src":"/(.*)",
            "dest":"try.ts"
        }
    ]
}